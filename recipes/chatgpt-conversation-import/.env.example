# Open Brain — ChatGPT Import
# Copy this file to .env and fill in your values.
# Then: export $(cat .env | xargs)

# Convex Open Brain API (default)
OB1_API_URL=https://YOUR_CONVEX_DEPLOYMENT.convex.site
OB1_API_KEY=your-mcp-access-key-here

# OpenRouter (for LLM summarization and embedding generation)
# If you rotate this key on openrouter.ai, update it here AND in Supabase secrets:
#   supabase secrets set OPENROUTER_API_KEY=sk-or-v1-your-new-key
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Custom ingest endpoint mode uses the Convex-compatible /capture endpoint.
INGEST_URL=https://YOUR_CONVEX_DEPLOYMENT.convex.site/capture
INGEST_KEY=your-mcp-access-key-here

# Legacy Supabase validation path:
# SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: User ID for multi-tenant RLS (required for --store-conversations)
# Find your user UUID in Supabase Auth > Users, or leave blank for single-user setups.
# USER_ID=your-user-uuid-here

# --store-conversations flag
# Stores conversation-level pyramid summaries in the chatgpt_conversations table.
# You MUST run schema.sql in your Supabase SQL Editor before using this flag.
# See: recipes/chatgpt-conversation-import/schema.sql
